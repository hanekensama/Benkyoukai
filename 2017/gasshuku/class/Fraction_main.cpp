#include <iostream>
#include <vector>
#include "Fraction.hpp"

using namespace std;

int main() {
  vector<Fraction> v;
  int a, b;

  while (cin >> a >> b, a != 0) {
    v.push_back(Fraction(a, b));
  }

  for ()
}

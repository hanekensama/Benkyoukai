#include <bits/stdc++.h>
using namespace std;
int solve(int n, vector<int>& w) {
  vector<vector<int> >dp(n, vector<int>(n));
  for () {
  }
}

int main() {
  int n;
  while(cin >> n, n != 0) {
    vector<int> w(n);
    for (int i = 0, i < n; i++) cin >> w[i];
    cout << solve(n, w) << endl;
  }
}
